//! Toolchain integration boundary for host tools embedded in the final Flatpak.
//!
//! The first server milestone is covered by integration tests that exercise the
//! tools directly. Runtime orchestration APIs should grow here as those tests
//! start driving product behavior instead of availability checks.
