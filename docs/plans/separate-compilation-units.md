# Separar 'Compilation Units'
Após ter o 'compile_commands.json', precisamos criar o processo de 'mock' de cada um dos 'compilation units'. Ou seja, criar 'mocks' de tudo o que cada 'compilation unit' precisa, para que o teste seja isolado.
